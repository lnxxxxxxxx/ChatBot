import '../styles.css';

function MyApp({ Component, pageProps }) {
  // Aquí puedes agregar cualquier lógica adicional para tu aplicación
  return <Component {...pageProps} />;
}

export default MyApp;
